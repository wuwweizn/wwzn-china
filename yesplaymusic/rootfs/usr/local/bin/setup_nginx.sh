# =============================================================================
# rootfs/usr/local/bin/setup_nginx.sh (可选，现在已内嵌到Dockerfile)
# =============================================================================
#!/bin/bash
echo "Nginx setup is now handled in the main startup script."